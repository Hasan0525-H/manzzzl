# Build APK

1. Open apps/android in Android Studio.
2. Sync Gradle.
3. Run:

./gradlew assembleDebug

Output:
app/build/outputs/apk/debug/app-debug.apk

Before release APK, add signing configuration and complete production dependencies.
